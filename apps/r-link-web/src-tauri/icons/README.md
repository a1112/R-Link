# R-Link 图标

`source.svg` 是本项目的可编辑矢量图标源。PNG、ICO、ICNS 由 Tauri CLI 生成：

```sh
# 在 apps/r-link-web 目录
npm run tauri -- icon src-tauri/icons/source.svg --output src-tauri/icons
```

提交桌面所需文件；移动平台输出可在需要时重新生成。
