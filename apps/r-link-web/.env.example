# Copy to .env.local. Never set a service-role key in browser variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Web development uses the loopback Vite proxy to port 8210.
# Set an explicit API URL for deployed static builds; Tauri defaults to port 8210.
VITE_API_BASE_URL=
