# Optional API URL. Local Web development uses the Vite proxy to port 8210.
# Tauri defaults to http://127.0.0.1:8210.
# Set before building a deployed static client; never embed service keys here.
VITE_API_BASE_URL=
