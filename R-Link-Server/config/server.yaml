# 配置示例；当前入口使用 main.py 与环境变量，不自动加载本文件。
# R-Link-Server 配置文件

server:
  host: "127.0.0.1"
  port: 8210
  reload: false  # 开发模式，生产环境设置为 false

plugins:
  dir: "plugins"
  auto_start: []  # 自动启动的插件列表

logging:
  level: "INFO"
  file: "logs/server.log"
